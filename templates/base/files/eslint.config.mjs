// managed-by: @jabelic/standards
// TODO: teach `standards apply` to add target-repo dependencies.
// This template currently assumes the target repository also installs
// `@jabelic/eslint-config`.
import baseConfig from "@jabelic/eslint-config/base";

export default baseConfig;
